<UserControl x:Class="fcb_public.sub_roll"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="400" xmlns:my="clr-namespace:fcb_public" Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <my:publicDataSet x:Key="publicDataSet" />
        <CollectionViewSource x:Key="rollViewSource" Source="{Binding Path=roll, Source={StaticResource publicDataSet}}" />
    </UserControl.Resources>
    <Grid DataContext="{StaticResource rollViewSource}">
        <DataGrid AutoGenerateColumns="False" EnableRowVirtualization="True" Height="141" HorizontalAlignment="Left" ItemsSource="{Binding}" Margin="0,26,0,0" Name="rollDataGrid" RowDetailsVisibilityMode="VisibleWhenSelected" VerticalAlignment="Top" Width="400">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="iDColumn" Binding="{Binding Path=ID}" Header="ID" Width="SizeToHeader" Visibility="Hidden" />
                <DataGridTextColumn x:Name="titleColumn" Binding="{Binding Path=title}" Header="标题" Width="SizeToHeader" />
                <DataGridTextColumn x:Name="txtColumn" Binding="{Binding Path=txt}" Header="内容" Width="SizeToHeader" />
            </DataGrid.Columns>
        </DataGrid>


        <Label Margin="0,181,0,139" Height="30" Width="60" HorizontalAlignment="Left" >标题</Label>
        <Label Margin="0,231,0,89" Height="30" Width="60" HorizontalAlignment="Left" >内容</Label>
        <TextBox Name="title_textbox" Height="30" Width="300" Margin="66,183,34,137" Text="{Binding Path=title, Mode=TwoWay, ValidatesOnExceptions=true, NotifyOnValidationError=true}"></TextBox>
        <TextBox Name="txt_textbox" Width="300" Margin="66,232,34,47" Text="{Binding Path=txt, Mode=TwoWay, ValidatesOnExceptions=true, NotifyOnValidationError=true}"></TextBox>
        <Button Name="roll_quedin" Margin="319,320,0,0" HorizontalAlignment="Left" Height="30" Width="30" Click="roll_quedin_Click">添加</Button>
        <Button Name="show_roll" Margin="241,320,0,0" HorizontalAlignment="Left" Height="30" Width="30" Click="show_roll_Click">展示</Button>
        <Grid HorizontalAlignment="Left" Margin="63,317,0,0" Name="grid1" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBox Grid.Column="1" Height="23" HorizontalAlignment="Left" Margin="1,3,0,6" Name="iDTextBox" Text="{Binding Path=ID, Mode=TwoWay, ValidatesOnExceptions=true, NotifyOnValidationError=true}" VerticalAlignment="Center" Width="120" />
        </Grid>
    </Grid>
</UserControl>
